# Toora — copy to .env and fill values. Never commit .env.

# OpenRouter (LLM)
OPENROUTER_API_KEY=

# Database (PostgreSQL from Railway)
DATABASE_URL=postgresql://user:password@host:port/railway

# Redis (from Railway)
REDIS_URL=redis://default:password@host:port

# Encrypt credentials in DB (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=

# Optional: Telegram webhook secret for verification
# TELEGRAM_WEBHOOK_SECRET=
