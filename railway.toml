# Toora — Root Railway configuration for Python services.
# web/backend service: start command is set in Railway dashboard as:
#   sh -c "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
# bot service: sh -c "uvicorn bot.main:app --host 0.0.0.0 --port $PORT"
# worker service: python worker/main.py
# frontend service: overridden by frontend/railway.toml (uses Nixpacks)

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
